name: LLM Quality Gates

on:
  workflow_dispatch:
  pull_request:
    paths:
      - golden-sets/**
      - regtrace.config.yaml
      - .github/workflows/regtrace.yml
  push:
    branches: [main]
    paths:
      - golden-sets/**
      - regtrace.config.yaml
      - .github/workflows/regtrace.yml

jobs:
  evaluate:
    permissions:
      contents: read
      pull-requests: write
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install regtrace
        run: |
          curl -L -o /usr/local/bin/regtrace \
            https://github.com/decimozs/regtrace/releases/latest/download/regtrace-linux-x64
          chmod +x /usr/local/bin/regtrace
          regtrace --version

      - name: Cache regression history
        uses: actions/cache@v4
        with:
          path: .regtrace
          key: regtrace-${{ hashFiles('golden-sets/**', 'regtrace.config.yaml') }}

      - name: Dry-run validation
        run: regtrace run --dry-run

      - name: Generate & Evaluate
        run: regtrace run --ci --generate --format markdown --output report.md
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}

      - name: Upload report
        uses: actions/upload-artifact@v4
        with:
          name: regtrace-report
          path: report.md

      - name: Post PR comment
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const report = fs.readFileSync('report.md', 'utf8');
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `## Regtrace Evaluation\n\n${report}`
            });

